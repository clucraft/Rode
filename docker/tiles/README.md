# Offline chart tiles

The optional `tiles` compose profile runs TileServer-GL against the
`rode-tiles` named volume. Put one or more `.mbtiles` files in that volume
and restart the profile; the web app is pointed at it via `RODE_TILES_URL`.

Filled in during phase 9. Until then this directory only documents the intent.
