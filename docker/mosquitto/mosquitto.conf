# Minimal broker for the optional `mqtt` profile.
# Anonymous access is allowed because the broker is only reachable on the boat
# LAN / tailnet. Set a password file here if that ever stops being true.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
log_type error
log_type warning
