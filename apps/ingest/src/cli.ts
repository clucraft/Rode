#!/usr/bin/env node
// rode-sim: run the NMEA simulator as a fake Cortex, or record/replay streams.
// Filled in during phase 2. Present now so `make sim` has a target.

console.error('rode-sim: simulator not implemented yet (phase 2)');
process.exit(2);
