{
  "name": "@rode/protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types and zod schemas for the REST + WebSocket contract",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@rode/core": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
